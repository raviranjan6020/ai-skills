# Global instructions (Claude Code)

@~/ai-skills/AGENTS.md

Topic rules load automatically from `~/.claude/rules/` (symlinked to `~/ai-skills/rules/`).
Skills: `/oss-pr`, `/go-review`. Agent: `security-reviewer`.
